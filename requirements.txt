# Install the CUDA-compatible PyTorch wheel first when using a GPU server.
torch>=2.1
torchvision>=0.16

# Project runtime
h5py>=3.10
numpy>=1.26
PyYAML>=6.0
scipy>=1.11

# Pretrained backbones and OpenAI CLIP ViT-B/16 guidance
clip @ git+https://github.com/openai/CLIP.git
einops>=0.8
mmengine>=0.10
open-clip-torch>=2.24
timm>=1.0
